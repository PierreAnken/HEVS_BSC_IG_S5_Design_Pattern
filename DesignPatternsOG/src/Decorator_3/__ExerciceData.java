package Decorator_3;

public class __ExerciceData {

/*    We have the following interface:

    public interface Shape {
        public void draw();
    }

    Create an application that allows creating shapes. You have different shapes,
    such as circles (with specific diameter), rectangles (with specific length and width)
    and squares (with specific width). You should be able to add new functionalities
    dynamically to shapes, such as borders (with different thicknesses), textures or colors.*/

}
